# demo.png

A layout mockup of the Gradio UI, rendered from `app.py`'s actual structure.

**What is real in it:** the generated SQL and every result row are genuine output
from the committed warehouse — the reference query for "Which providers have the
worst clean claim rate?" executed against `data/claims_warehouse.duckdb`.

**What is not:** a model did not produce that SQL. It is the reference query.

Replace this with a live screenshot once the model is trained. The image carries a
footer saying so, so a reader is never misled about which part is measured.

The screenshot from the inventory version of this project is deliberately not reused:
it shows a different schema and a different app.
