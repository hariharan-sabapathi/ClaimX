.venv/
__pycache__/
*.py[cod]
.env
.ipynb_checkpoints/

# Built artefacts. The warehouse is regenerated by `python warehouse.py` and
# the datasets by `python prepare_dataset.py`, so neither belongs in git.
data/claims_warehouse.duckdb
data/train.jsonl
data/eval.jsonl

# CMS DE-SynPUF extracts, if you download them. Large, and redistributing them
# from a personal repo is not the intent.
data/raw/

# Eval output is committed selectively — keep the run you cite in the README.
eval/results/*.json
!eval/results/README.md
